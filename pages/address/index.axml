<view class="address">
  <view class="title">我的地址</view>
  <view class="list">
    <address-card a:for="{{address.data}}" data="{{item}}" index="{{index}}" menu="{{true}}" />
  </view>
  <view class="add" onTap="onBuild">新增地址</view>
</view>