
<view class="page-order-list">
  <view class="title">我的订单</view>
  <view class="nav">
    <view a:for="{{orderList.type.list}}" data="{{item}}" class="{{[orderList.type.current == item.value && 'select-nav']}}" data-value="{{item.value}}" onTap="changeOrderType">{{item.label}}</view>
  </view>
  <order-list order="{{orderList}}" />
</view>

